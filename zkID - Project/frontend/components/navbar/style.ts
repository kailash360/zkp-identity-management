import { variables } from "@components/styles/variable";

export const navbar = {
    background: variables.background.primary,
}

export const navbarText = {
    color: variables.text.secondary,
    fontWeight: "bold",
    fontSize: "30px"
}

export const userText = {
    color: "#fff"
}